# P4 -- mixed 2x2

Blocked until `../tp2/` and `../pp2/` each have a passing
correctness + health run on this host with the current UMD.

Question (when unblocked): is TP=2 inside a two-stage pipeline, or
PP=2 of two TP=1 ranks, a real map on this PCI tree, or a paper map
that dies in collectives?

Open. Run those two layouts as separate experiments. Do not invent a
third topology until both have a verdict.
